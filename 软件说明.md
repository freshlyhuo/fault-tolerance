# 软件说明（面向使用者与协作者）

## 一、软件概述

本软件用于卫星业务系统与微服务系统的故障闭环处理，覆盖“监测-预警-诊断-修复-验证”全过程。

软件在系统中的定位如下：

- 前向输入：接收业务遥测数据、容器与服务运行数据。
- 本系统职责：识别异常、判断故障原因、执行修复动作、输出处置结果。
- 后向输出：向运维/上注/编排模块输出告警、诊断结果、修复状态与日志。
- 左右协同：
	- 与健康监测协同，形成统一告警入口。
	- 与故障诊断协同，将告警映射为故障码。
	- 与故障修复协同，执行动作并回传结果。
	- 与配置管理协同，支持阈值、策略、模型在线调整。

从使用者角度，软件价值是：当系统出现温度、电压、容器、服务等异常时，系统自动给出“发生了什么、影响了什么、已采取什么措施、当前是否恢复”的可追踪结论。

## 二、软件功能（使用者视角）

以下功能均从“用户能做什么、遇到问题会发生什么”描述，不涉及内部调度算法。

### 2.1 软件获取与安装

- 支持通过发布包下载部署（地面运维环境）或离线介质（如 U 盘）导入部署。
- 支持在 SylixOS 目标机部署可执行文件并独立启动健康监测、故障诊断、故障修复进程。
- 支持通过 JSON 配置文件加载监测对象、阈值、故障树和修复策略。

部署方式（建议流程）：

1. 准备部署产物：
	 - 主程序二进制（示例）：
		 - `build/health-monitor-sylixos`
		 - `fault-diagnosis/build/fault-diagnosis-sylixos`
		 - `fault-recovery` 对应可执行程序（按实际编译产物命名）
2. 拷贝到目标目录：
	 - 建议目录：`/usr/local/bin`（程序）和 `/etc/fault-tolerance`（配置）
3. 下发并校验配置文件：
	 - 基础业务监测配置：
		 - `power.json`
		 - `thermal.json`
		 - `comm.json`
		 - `actuator.json`
	 - 诊断配置：
		 - `fault-diagnosis/configs/fault_tree_business.json`
		 - `fault-diagnosis/configs/fault_tree_microservice.json`
	 - 修复配置：
		 - `fault-recovery/configs/recovery_service_config.json`
4. 启动服务并检查结果：
	 - 检查进程是否存活、日志是否持续输出、配置是否加载成功。
	 - 若配置缺失或字段错误，程序应在启动日志中给出明确报错并拒绝进入正常监测流程。

使用者可感知结果：安装部署完成后，可看到服务进程状态、版本信息、配置加载结果和基础运行日志。

### 2.2 健康监测

- 支持业务层监测：温度、电压、电流等遥测参数持续采集。
- 支持微服务层监测：容器状态、服务状态、CPU/内存等资源指标监测。
- 支持阈值越界告警与异常事件生成。

典型场景：

- 当温度持续超限时，系统自动生成告警并记录时间、对象、异常项。
- 当容器异常退出时，系统自动上报告警并进入后续诊断流程。

### 2.3 故障预测（预警）

- 支持基于历史趋势的风险提示。
- 支持在“未故障但持续恶化”阶段提前预警。

典型场景：

- 当 CPU 使用率持续上升且接近阈值时，系统提前提示潜在故障风险。

### 2.4 故障诊断

- 支持对告警进行故障树规则推理。
- 支持输出结构化诊断结果：故障码、故障类型、触发原因、影响范围。
- 支持业务层与微服务层双域诊断。

使用者可感知结果：收到明确故障结论，而不是仅有原始告警。

### 2.5 故障修复

- 支持按故障码匹配修复策略并自动执行。
- 支持业务层修复动作（如拉起指定镜像容器）。
- 支持微服务层修复动作（如重启服务、熔断限流、恢复策略）。
- 支持失败重试、超时终止、升级人工处理。

典型场景：

- 当服务无响应时，系统可自动尝试重启服务；若多次失败，则上报人工介入。

### 2.6 修复结果验证与闭环

- 支持修复后持续观测关键指标。
- 支持输出修复结果状态：成功、失败、超时。
- 支持记录完整处置链路日志，便于复盘与审计。

使用者可感知结果：可确认“是否恢复正常”，并可查看失败原因。

### 2.7 配置管理

- 支持修改监测周期、阈值、故障模型、修复策略映射。
- 支持配置热更新或重启生效（由具体配置项定义）。
- 支持配置文件集中管理，便于版本化维护。

## 三、交互关系（写给协作者）

本节用于对接方快速理解接口边界、输入输出与责任划分。

### 3.1 上下游关系

- 上游模块：遥测采集模块、ECSM/编排平台、配置下发模块。
- 本模块：健康监测、预测预警、故障诊断、故障修复、状态回报。
- 下游模块：运维展示、日志审计、人工处置平台。

### 3.2 关键交互对象

- 告警事件（AlertEvent）
	- 输入来源：健康监测。
	- 关键字段：event_id、timestamp、source、metric、severity、message。
- 诊断结果（DiagnosisResult）
	- 输出来源：故障诊断。
	- 关键字段：fault_code、fault_type、root_cause、affected_scope。
- 修复指令（RecoveryAction）
	- 输入来源：故障修复引擎。
	- 关键字段：fault_code、action_type、target、timeout、retry_policy。
- 修复反馈（RecoveryResult）
	- 输出来源：故障修复执行器。
	- 关键字段：status、start_time、end_time、detail、next_step。

### 3.3 交互方式建议

- 进程内：Go channel/接口回调。
- 跨进程：消息队列或共享存储订阅（按工程部署选型）。
- 对外文档：统一维护接口字段表、错误码表、版本变更记录。

## 四、架构设计

### 4.1 总体架构

系统采用分层闭环架构：

1. 数据采集层：采集业务遥测与微服务运行指标。
2. 监测与预警层：阈值检测、趋势检测、告警生成。
3. 诊断层：基于故障树进行逻辑推理，输出故障码。
4. 修复执行层：按策略执行恢复动作并管理过程状态。
5. 验证与反馈层：校验修复结果并回写系统状态。
6. 配置与运维层：策略配置、日志追踪、审计复盘。

### 4.2 架构设计关键点（防遗忘清单）

- 统一事件模型：告警、诊断、修复对象字段保持一致命名。
- 状态机约束：HEALTHY/ALERTING/DIAGNOSING/RECOVERING/FAILED 状态流转明确。
- 幂等与防重：同一故障重复上报时避免重复修复。
- 超时与看门狗：每个修复动作必须有超时控制。
- 可观测性：日志、指标、链路追踪需覆盖关键路径。
- 可配置性：阈值、周期、策略映射不硬编码。
- 安全性：关键动作需权限校验与操作留痕。
- 可回滚性：修复动作失败时支持安全回退或人工接管。

## 五、详细设计（建议落地格式）

详细设计可写在设计文档或代码注释中，但建议至少覆盖以下内容。

### 5.1 模块级详细设计模板

- 模块名称：
- 输入：
- 输出：
- 核心流程：
- 状态变化：
- 异常处理：
- 超时与重试：
- 配置项：
- 日志与指标：
- 测试点：

### 5.2 接口级详细设计模板

- 接口名：
- 调用方/被调方：
- 请求字段定义：
- 响应字段定义：
- 错误码：
- 幂等策略：
- 时序图/状态图：

## 六、软件功能描述怎么写（编写规范）

为避免“只讲内部实现”，功能描述建议统一采用以下句式：

- 用户能做什么：支持什么操作（安装、配置、启动、查询、处置）。
- 出问题时会怎样：系统自动触发什么告警/诊断/修复。
- 用户能看到什么：结果页面、日志、状态码、提示信息。
- 失败时怎么办：重试、降级、人工接管、可追踪记录。

推荐写法示例：

- 不推荐：系统使用某算法进行任务调度。
- 推荐：当服务响应超时超过阈值时，系统会自动重启目标服务；若连续失败 3 次，将触发人工处理告警并保留完整日志。

按以上模板编写后，文档能同时满足使用者、协作者和后续维护人员阅读需求。